The database is called twiceme and uses the user twiceme_admin. Below is a command to run the script that will init everything:  

linux:
sudo -u postgres psql -f setup.sql

mac (untested): 
psql -f setup.sql

